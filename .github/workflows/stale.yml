name: Stale
on:
  schedule:
    - cron: 0 11 * 1-2,11-12 1
    - cron: 0 10 * 3-10 1

jobs:
  stale:
    name: Stale
    uses: Katsute/Workflows/.github/workflows/stale.yml@main
    secrets:
      token: ${{ secrets.BOT }}